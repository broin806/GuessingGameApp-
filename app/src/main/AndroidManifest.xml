<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="project.st01367734.broinson">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MediumLevelIntro" />
        <activity android:name=".HardLevelIntro" />
        <activity android:name=".EasyLevelIntro" />
        <activity android:name=".OutputGuessesHardLevel" />
        <activity android:name=".OutputGuessesMediumLevel" />
        <activity android:name=".OutputGuessesEasyLevel" />
        <activity android:name=".HardLevel" />
        <activity android:name=".MediumLevel" />
        <activity android:name=".EasyLevel" />
        <activity android:name=".WelcomePage" />
        <activity android:name=".MainActivity"/>
        <activity android:name=".MainActivity2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
